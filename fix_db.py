import urllib.request
import ssl
# We don't have python pymongo locally, wait, I can just use mongo shell! Wait, mongo shell was not found earlier (`zsh:1: command not found: mongo`).
# Let's write a rust script, but wait! I can just use mongosh!
